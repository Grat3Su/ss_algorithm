package BOJ;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class BOJ_563_색종이 {

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		int T = Integer.parseInt(br.readLine());
		int[] x = new int[T];
		int[] y = new int[T];
		int result = 0;//겹치는 부분
		for(int t = 0; t<T; t++) {
			StringTokenizer st = new StringTokenizer(br.readLine());
			x[t] = Integer.parseInt(st.nextToken());
			y[t] = Integer.parseInt(st.nextToken());
		}
		
		for(int i = 0; i<T; i++) {
			for(int j = 0; j<T; j++) {
				if((x[i] < x[j] &&x[i]+10<x[j])&&(y[i] < y[j] &&y[i]+10<y[j])) {
					int num = (x[i]-x[j])*(y[i]-y[j]);
					result+= num>0?num:num*-1;
					
				}
			}
		}
		
		System.out.println(10*T-result);
	}

}
